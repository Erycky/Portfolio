<section class="about-section" id="sobre">
  <div class="about-container">
    <div class="about-left">
      <div class="profile-tag">
        <span class="orange-brackets">&lt; &gt;</span> Erycky Rayner
      </div>
      <h2 class="headline-text">
        Apaixonado por tecnologia e design, criar a forma e dar a ela função me
        cativa.
      </h2>
    </div>

    <div class="about-right">
      <h3 class="sub-headline-text">
        Cada linha de código HTML, cada estilo em CSS e cada interação em
        JavaScript refletem meu compromisso em criar experiências digitais
        cativantes e funcionais.
      </h3>
      <p class="paragraph-text">
        Para mim, o desenvolvimento front end não é apenas uma habilidade, é uma
        forma de arte, onde busco constantemente aprender e inovar para
        construir interfaces web que encantem e facilitem a vida dos usuários.
      </p>
    </div>
  </div>

  <div class="about-divider">
    <span>&lt; &gt;</span>
  </div>
</section>

<style>
  .about-section {
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
    padding: 8rem 4rem 4rem 4rem;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .about-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    width: 100%;
    align-items: start;
  }

  .about-left {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .profile-tag {
    font-family: var(--font-main);
    font-size: 1.15rem;
    font-weight: 500;
    color: #000000;
  }

  .orange-brackets {
    color: var(--color-primary);
    font-weight: 700;
    margin-right: 0.25rem;
  }

  .headline-text {
    font-family: var(--font-main);
    font-size: 2.2rem;
    font-weight: 800;
    line-height: 1.3;
    color: #000000;
  }

  .about-right {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding-top: 2.5rem;
  }

  .sub-headline-text {
    font-family: var(--font-main);
    font-size: 1.4rem;
    font-weight: 800;
    line-height: 1.4;
    color: #000000;
  }

  .paragraph-text {
    font-family: var(--font-main);
    font-size: 1.05rem;
    font-weight: 500;
    line-height: 1.6;
    color: #333333;
  }

  .about-divider {
    margin-top: 6rem;
    font-size: 2rem;
    font-weight: 900;
    color: var(--color-primary);
    letter-spacing: 2px;
    text-align: center;
    width: 100%;
  }

  @media (max-width: 968px) {
    .about-container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .about-right {
      padding-top: 0;
    }

    .headline-text {
      font-size: 1.8rem;
    }

    .sub-headline-text {
      font-size: 1.25rem;
    }

    .about-section {
      padding: 5rem 2rem 3rem 2rem;
    }

    .about-divider {
      margin-top: 4rem;
    }
  }
</style>
